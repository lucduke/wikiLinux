# Suppression des bloatwares sur Windows 11

## Installation

On lance PowerShell en mode admin et on execute la commande suivante :

```Powershell
& ([scriptblock]::Create((irm "https://debloat.raphi.re/")))
```
